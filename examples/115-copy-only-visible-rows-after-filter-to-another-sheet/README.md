# 115. Copy only visible rows (after filter) to another sheet

**What it does:** Copy only visible rows (after filter) to another sheet.

**Notes / Requirements:** Run from Apps Script editor; bind to the relevant container app as needed.

## Code
See: [`Code.gs`](./Code.gs)
