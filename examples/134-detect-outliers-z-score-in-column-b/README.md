# 134. Detect outliers (z-score) in column B

**What it does:** Detect outliers (z-score) in column B.

**Notes / Requirements:** Run from Apps Script editor; bind to the relevant container app as needed.

## Code
See: [`Code.gs`](./Code.gs)
