# 200. End-to-end: read sheet → create doc → save PDF → email link

**What it does:** End-to-end: read sheet → create doc → save PDF → email link.

**Notes / Requirements:** Requires Gmail authorization. Requires Drive authorization. Requires Docs authorization (Docs-bound recommended where applicable).

## Code
See: [`Code.gs`](./Code.gs)
