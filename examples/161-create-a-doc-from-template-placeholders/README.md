# 161. Create a Doc from template placeholders

**What it does:** Create a Doc from template placeholders.

**Notes / Requirements:** Replace placeholder IDs like `PASTE_*` before running. Requires Drive authorization. Requires Docs authorization (Docs-bound recommended where applicable).

## Code
See: [`Code.gs`](./Code.gs)
