# 195. Fetch JSON with timeout + error handling

**What it does:** Fetch JSON with timeout + error handling.

**Notes / Requirements:** Requires UrlFetch authorization (external requests).

## Code
See: [`Code.gs`](./Code.gs)
