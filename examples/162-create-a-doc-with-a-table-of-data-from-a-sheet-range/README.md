# 162. Create a doc with a table of data from a sheet range

**What it does:** Create a doc with a table of data from a sheet range.

**Notes / Requirements:** Requires Docs authorization (Docs-bound recommended where applicable).

## Code
See: [`Code.gs`](./Code.gs)
