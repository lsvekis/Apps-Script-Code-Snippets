# 191. Generate an HMAC signature (Webhook signing)

**What it does:** Generate an HMAC signature (Webhook signing).

**Notes / Requirements:** Run from Apps Script editor; bind to the relevant container app as needed.

## Code
See: [`Code.gs`](./Code.gs)
