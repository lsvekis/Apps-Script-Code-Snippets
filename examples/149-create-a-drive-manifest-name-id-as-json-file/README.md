# 149. Create a Drive “manifest” (name → id) as JSON file

**What it does:** Create a Drive “manifest” (name → id) as JSON file.

**Notes / Requirements:** Replace placeholder IDs like `PASTE_*` before running. Requires Drive authorization.

## Code
See: [`Code.gs`](./Code.gs)
