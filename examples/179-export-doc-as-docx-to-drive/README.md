# 179. Export doc as DOCX to Drive

**What it does:** Export doc as DOCX to Drive.

**Notes / Requirements:** Requires Drive authorization. Requires Docs authorization (Docs-bound recommended where applicable). Requires UrlFetch authorization (external requests).

## Code
See: [`Code.gs`](./Code.gs)
