# 109. Create a filter view-style filter (basic filter)

**What it does:** Create a filter view-style filter (basic filter).

**Notes / Requirements:** Run from Apps Script editor; bind to the relevant container app as needed.

## Code
See: [`Code.gs`](./Code.gs)
