# 181. Create a Gmail label hierarchy (Label/Sub)

**What it does:** Create a Gmail label hierarchy (Label/Sub).

**Notes / Requirements:** Requires Gmail authorization.

## Code
See: [`Code.gs`](./Code.gs)
