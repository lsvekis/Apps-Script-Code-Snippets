# 130. Convert currency strings to numbers (e.g., "$1,234.56")

**What it does:** Convert currency strings to numbers (e.g., "$1,234.56").

**Notes / Requirements:** Run from Apps Script editor; bind to the relevant container app as needed.

## Code
See: [`Code.gs`](./Code.gs)
