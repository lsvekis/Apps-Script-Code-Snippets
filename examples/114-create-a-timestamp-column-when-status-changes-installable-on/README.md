# 114. Create a timestamp column when status changes (installable onEdit)

**What it does:** Create a timestamp column when status changes (installable onEdit).

**Notes / Requirements:** Trigger: Use an installable trigger for reliability.

## Code
See: [`Code.gs`](./Code.gs)
