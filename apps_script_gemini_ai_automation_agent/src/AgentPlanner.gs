function createAgentPlan(r){if(!r||!r.trim())throw new Error("Enter a request.");const t=getAvailableTools_();const q=`You are a Google Workspace automation planner. Use only these tools: ${JSON.stringify(t)}. Return JSON only: {"goal":"","steps":[{"tool":"","reason":""}]}. Use minimum tools in execution order. readSheetData before analyzeData; analyzeData before document/email tools. Do not execute anything. USER REQUEST: ${r}`;const p=JSON.parse(cleanJsonResponse_(callGemini(q,"")));validateAgentPlan_(p);return p;}